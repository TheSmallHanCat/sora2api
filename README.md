# Sora2API

<div align="center">

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.8%2B-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/fastapi-0.119.0-green.svg)](https://fastapi.tiangolo.com/)
[![Docker](https://img.shields.io/badge/docker-supported-blue.svg)](https://www.docker.com/)

**Полнофункциональный API-сервис, совместимый с OpenAI, предоставляющий единый интерфейс для Sora**

</div>

---

## 📋 Содержание

- [Возможности](#возможности)
- [Быстрый старт](#быстрый-старт)
- [Руководство по использованию](#руководство-по-использованию)
  - [Краткий справочник](#краткий-справочник)
  - [Панель управления](#панель-управления)
  - [API-вызовы](#api-вызовы)
  - [Функция видео-персонажей](#функция-видео-персонажей)
- [Лицензия](#лицензия)

---

## ✨ Возможности

### Основные функции
- 🎨 **Текст в изображение** - Генерация изображений по текстовому описанию
- 🖼️ **Изображение в изображение** - Креативная трансформация загруженных изображений
- 🎬 **Текст в видео** - Генерация видео по текстовому описанию
- 🎥 **Изображение в видео** - Генерация видео на основе изображения
- 📊 **Поддержка разных размеров** - Горизонтальный, вертикальный и другие форматы
- 🎭 **Функция видео-персонажей** - Создание персонажей и генерация видео с ними
- 🎬 **Функция Remix** - Продолжение творчества на основе существующего видео

### Расширенные возможности
- 🔐 **Управление токенами** - Поддержка нескольких токенов и балансировка нагрузки
- 🌐 **Поддержка прокси** - Поддержка HTTP и SOCKS5 прокси
- 📝 **Детальные логи** - Полное логирование запросов/ответов
- 🔄 **Асинхронная обработка** - Эффективная асинхронная обработка задач
- 💾 **Сохранение данных** - Хранение в базе данных SQLite
- 🎯 **Совместимость с OpenAI** - Полная совместимость с форматом OpenAI API
- 🛡️ **Безопасная аутентификация** - Проверка API-ключей и управление правами
- 📱 **Веб-интерфейс управления** - Удобная панель администратора

---

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose (рекомендуется)
- Или Python 3.8+

### Способ 1: Развёртывание через Docker (рекомендуется)

#### Стандартный режим (без прокси)

```bash
# Клонировать проект
git clone https://github.com/TheSmallHanCat/sora2api.git
cd sora2api

# Запустить сервис
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

#### Режим WARP (с прокси)

```bash
# Запуск с прокси WARP
docker-compose -f docker-compose.warp.yml up -d

# Просмотр логов
docker-compose -f docker-compose.warp.yml logs -f
```

### Способ 2: Локальное развёртывание

```bash
# Клонировать проект
git clone https://github.com/TheSmallHanCat/sora2api.git
cd sora2api

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить сервис
python main.py
```

### Первый запуск

После запуска сервиса откройте панель управления для начальной настройки:

- **Адрес**: http://localhost:8000
- **Имя пользователя**: `admin`
- **Пароль**: `admin`

⚠️ **Важно**: Обязательно смените пароль после первого входа!

---

### Краткий справочник

| Функция | Модель | Описание |
|---------|--------|----------|
| Текст в изображение | `sora-image*` | Используйте `content` как строку |
| Изображение в изображение | `sora-image*` | Используйте массив `content` + `image_url` |
| Текст в видео | `sora-video*` | Используйте `content` как строку |
| Изображение в видео | `sora-video*` | Используйте массив `content` + `image_url` |
| Создание персонажа | `sora-video*` | Используйте массив `content` + `video_url` |
| Генерация видео с персонажем | `sora-video*` | Используйте массив `content` + `video_url` + текст |
| Remix | `sora-video*` | Включите Remix ID в `content` |
| Раскадровка видео | `sora-video*` | Используйте формат ```[длительность s]промпт``` в `content` |

---

### API-вызовы

#### Основная информация (стандартный формат OpenAI, требуется потоковый режим)

- **Эндпоинт**: `http://localhost:8000/v1/chat/completions`
- **Аутентификация**: Добавьте в заголовок `Authorization: Bearer YOUR_API_KEY`
- **API Key по умолчанию**: `han1234` (рекомендуется изменить)

#### Поддерживаемые модели

**Модели для изображений**

| Модель | Описание | Размер |
|--------|----------|--------|
| `sora-image` | Текст в изображение (по умолчанию) | 360×360 |
| `sora-image-landscape` | Текст в изображение (горизонтальный) | 540×360 |
| `sora-image-portrait` | Текст в изображение (вертикальный) | 360×540 |

**Модели для видео**

| Модель | Длительность | Ориентация | Описание |
|--------|--------------|------------|----------|
| `sora-video-10s` | 10 сек | Горизонтальная | Текст/изображение в видео |
| `sora-video-15s` | 15 сек | Горизонтальная | Текст/изображение в видео |
| `sora-video-landscape-10s` | 10 сек | Горизонтальная | Текст/изображение в видео |
| `sora-video-landscape-15s` | 15 сек | Горизонтальная | Текст/изображение в видео |
| `sora-video-portrait-10s` | 10 сек | Вертикальная | Текст/изображение в видео |
| `sora-video-portrait-15s` | 15 сек | Вертикальная | Текст/изображение в видео |

#### Примеры запросов

**Текст в изображение**

```bash
curl -X POST "http://localhost:8000/v1/chat/completions" \
  -H "Authorization: Bearer han1234" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-image",
    "messages": [
      {
        "role": "user",
        "content": "Милый котёнок"
      }
    ]
  }'
```

**Изображение в изображение**

```bash
curl -X POST "http://localhost:8000/v1/chat/completions" \
  -H "Authorization: Bearer han1234" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-image",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "text",
            "text": "Преобразовать это изображение в стиль масляной живописи"
          },
          {
            "type": "image_url",
            "image_url": {
              "url": "data:image/png;base64,<base64_encoded_image_data>"
            }
          }
        ]
      }
    ],
    "stream": true
  }'
```

**Текст в видео**

```bash
curl -X POST "http://localhost:8000/v1/chat/completions" \
  -H "Authorization: Bearer han1234" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-video-landscape-10s",
    "messages": [
      {
        "role": "user",
        "content": "Котёнок бегает по траве"
      }
    ],
    "stream": true
  }'
```

**Изображение в видео**

```bash
curl -X POST "http://localhost:8000/v1/chat/completions" \
  -H "Authorization: Bearer han1234" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-video-landscape-10s",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "text",
            "text": "Этот кот танцует"
          },
          {
            "type": "image_url",
            "image_url": {
              "url": "data:image/png;base64,<base64_encoded_image_data>"
            }
          }
        ]
      }
    ],
    "stream": true
  }'
```

**Remix видео (продолжение творчества на основе существующего видео)**

* Достаточно включить ссылку или ID remix в промпт

```bash
curl -X POST "http://localhost:8000/v1/chat/completions" \
  -H "Authorization: Bearer han1234" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-video-landscape-10s",
    "messages": [
      {
        "role": "user",
        "content": "https://sora.chatgpt.com/p/s_68e3a06dcd888191b150971da152c1f5 преобразовать в стиль тушевой живописи"
      }
    ]
  }'
```

**Раскадровка видео**

* Пример триггерного промпта:
  ```[5.0s]кот прыгает с парашютом с самолёта [5.0s]кот приземляется [10.0s]кот бежит по полю```
* Или
  ```text
  [5.0s]кот прыгает с парашютом с самолёта
  [5.0s]кот приземляется
  [10.0s]кот бежит по полю
  ```

```bash
curl -X POST "http://localhost:8000/v1/chat/completions" \
  -H "Authorization: Bearer han1234" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-video-landscape-10s",
    "messages": [
      {
        "role": "user",
        "content": "[5.0s]кот прыгает с парашютом с самолёта [5.0s]кот приземляется [10.0s]кот бежит по полю"
      }
    ]
  }'
```

### Функция видео-персонажей

Sora2API поддерживает функцию **генерации видео-персонажей**.

#### Описание функции

- **Создание персонажа**: Если есть только видео без промпта, система автоматически извлекает информацию о персонаже и выводит имя персонажа
- **Генерация с персонажем**: При наличии видео и промпта — загружается видео, создаётся персонаж, и с использованием персонажа и промпта генерируется видео

#### API-вызовы (стандартный формат OpenAI, требуется потоковый режим)

**Сценарий 1: Только создание персонажа (без генерации видео)**

Загрузка видео для извлечения информации о персонаже, получение имени и аватара персонажа.

```bash
curl -X POST "http://localhost:8000/v1/chat/completions" \
  -H "Authorization: Bearer han1234" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-video-landscape-10s",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "video_url",
            "video_url": {
              "url": "data:video/mp4;base64,<base64_encoded_video_data>"
            }
          }
        ]
      }
    ],
    "stream": true
  }'
```

**Сценарий 2: Создание персонажа и генерация видео**

Загрузка видео для создания персонажа, затем генерация нового видео с этим персонажем.

```bash
curl -X POST "http://localhost:8000/v1/chat/completions" \
  -H "Authorization: Bearer han1234" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-video-landscape-10s",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "video_url",
            "video_url": {
              "url": "data:video/mp4;base64,<base64_encoded_video_data>"
            }
          },
          {
            "type": "text",
            "text": "Персонаж танцует"
          }
        ]
      }
    ],
    "stream": true
  }'
```

#### Пример кода на Python

```python
import requests
import base64

# Чтение видеофайла и кодирование в Base64
with open("video.mp4", "rb") as f:
    video_data = base64.b64encode(f.read()).decode("utf-8")

# Только создание персонажа
response = requests.post(
    "http://localhost:8000/v1/chat/completions",
    headers={
        "Authorization": "Bearer han1234",
        "Content-Type": "application/json"
    },
    json={
        "model": "sora-video-landscape-10s",
        "messages": [
            {
                "role": "user",
                "content": [
                    {
                        "type": "video_url",
                        "video_url": {
                            "url": f"data:video/mp4;base64,{video_data}"
                        }
                    }
                ]
            }
        ],
        "stream": True
    },
    stream=True
)

# Обработка потокового ответа
for line in response.iter_lines():
    if line:
        print(line.decode("utf-8"))
```

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).

---

## 🙏 Благодарности

Спасибо всем участникам и пользователям за поддержку!

---

## 📞 Контакты

- Создать Issue: [GitHub Issues](https://github.com/TheSmallHanCat/sora2api/issues)
- Обсуждения: [GitHub Discussions](https://github.com/TheSmallHanCat/sora2api/discussions)

---

**⭐ Если этот проект был вам полезен, поставьте Star!**
